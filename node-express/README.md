# node-express

## Running Locally

* Postgres running locally
* Create a database called `node-express`
* `npm install`
* `npm start`

```
CREATE TABLE book (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255)
);
```
